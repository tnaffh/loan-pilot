import react from './react.mjs';

/**
 * Next.js apps extend the React config. The Next-specific plugin
 * (`eslint-config-next`) is layered in inside each app's own config so it can
 * resolve the app's local Next.js install.
 */
export default react;
